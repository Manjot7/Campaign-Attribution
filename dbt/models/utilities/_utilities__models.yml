version: 2

models:
  - name: metricflow_time_spine
    description: Day-grain time spine for MetricFlow, spanning the GA4 sample window.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
        tests: [not_null, unique]
