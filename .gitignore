# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Generated analysis artifacts (the executed notebook already embeds the chart)
analysis/*.png

# Unrelated personal file kept in this folder, not part of this project
Customer Churn Prediction.ipynb

# Local commit crib sheet (delete after use)
COMMIT_PLAN.txt

# Claude Code local settings
.claude/settings.local.json

# dbt
dbt/.user.yml
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/package-lock.yml

# Airflow (local standalone artifacts)
airflow/logs/
airflow/plugins/
airflow/.env
airflow/simple_auth_manager_passwords.json.generated

# Credentials — never commit
*.json.key
service-account*.json
.env

# OS / editor
.DS_Store
Thumbs.db
